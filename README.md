Switch user

```bash
su
```

```bash
pacman -Syyyy --noconfirm --needed  artix-archlinux-support


pacman-key --populate archlinux


pacman -Syyyy --noconfirm --needed xorg-server xorg-xinit libxinerama libxft feh unclutter unzip ttf-font-awesome ttf-hack git zsh
```

